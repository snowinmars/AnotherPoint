@using AnotherPoint.Common
@using AnotherPoint.Core
@model AnotherPoint.Entities.Ctor

@{
	string parameters = CtorCore.GetArgumentCollectionAsString(Model);
	string body = CtorCore.GetBodyAsString(Model);
}

@Model.AccessModifyer.AsString() @Model.Type.Name (@parameters)
{
	@body
}